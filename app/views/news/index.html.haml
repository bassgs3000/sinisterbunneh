.container-fluid
  .row-fluid
    %h1.offset5
      .underline 
        News
  .row-fluid
    .hero-unit.offset4.span4
      %ul
        - @news.reverse.each do |entry|
          %li
            = link_to entry.date.strftime("%B #{entry.date.day.ordinalize}, %Y - #{entry.title}"), news_path(entry)