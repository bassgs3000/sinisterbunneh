%h1 Listing locks

%table
  %tr
    %th Status
    %th
    %th
    %th

  - @locks.each do |lock|
    %tr
      %td= lock.status
      %td= link_to 'Show', lock
      %td= link_to 'Edit', edit_lock_path(lock)
      %td= link_to 'Destroy', lock, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Lock', new_lock_path
