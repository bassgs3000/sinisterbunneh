.hero-unit.span9
  - @posts.each_slice(4) do |post|
    .row-fluid
      - post.each do |img|
        %center.span3
          = link_to image_tag(img.image.url(:thumb)), post_path(img)
          %br/
          = link_to img.title, post_path(img)
  %center.row-fluid
    = will_paginate @posts, renderer: BootstrapPagination::Rails